{
    "server": "[::]:443",
    // UUID可执行cat /proc/sys/kernel/random/uuid生成
	  //Password可执行openssl rand -base64 32生成
    "users": {
        "UUID": "Password"
    },    
    "certificate": "/etc/tuic/cert.crt",  // 证书文件路径，绝对路径   
    "private_key": "/etc/tuic/private.key",  // 私钥文件的路径，绝对路径   
    "congestion_control": "bbr",  // 拥塞控制算法，可用选项："cubic", "new_reno", "bbr"
    "alpn": ["h3"],
    "log_level": "warn"
}
